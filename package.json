{
  "name": "updater",
  "version": "1.0.0",
  "description": "auto updater for znci",
  "main": "bin/index.js",
  "types": "bin/index.d.ts",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "start": "node bin/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -b -v",
    "nodemon": "npm run build && npm run start"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.0.3"
  }
}
